export type TInputMemberDTO = {
  user_id: number;
  party_id: number;
  is_admin: boolean;
};

export type TOutputMemberDTO = {
  id: number;
  user_id: number;
  party_id: number;
  is_admin: boolean;
};
